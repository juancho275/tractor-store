# sharedCatalog

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build sharedCatalog` to build the library.

## Running unit tests

Run `nx test sharedCatalog` to execute the unit tests via [Jest](https://jestjs.io).
